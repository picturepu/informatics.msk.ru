#include <iostream>
#include <bits/stdc++.h>

using namespace std;

int main()
{
    int x, length = -1;
    do
    {
        length++;
        cin>>x;
    }
    while (x);
    cout << length;
}
